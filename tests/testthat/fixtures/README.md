# Test fixtures

`sample-downloads.db` — a tiny SQLite database with rows for 5 well-known
packages across 2024–2026. Built during plan pre-work via:

    sqlite3 ... (see the plan's pre-work section)

Regenerate if the schema changes. Should stay under 1 MB.
