# Placeholder — keeps test_dir() happy before real tests arrive (Task 2+).
# Remove once test-touched-years.R (or any real test file) is added.
test_that("scaffold is wired up", {
  expect_true(TRUE)
})
